/// Below this width the app shell switches from a permanent sidebar to a
/// drawer, and pages switch their own internal layouts to a single column —
/// shared so both decisions flip at the same width instead of drifting.
const double kSidebarBreakpoint = 768;
